version: '3.4'

services:
  billingsystem:
    image: billingsystem
    build:
      context: egideHarerimana-backend
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: development
    ports:
      - 5000:5000
    command: ["node","./src/main.ts"]
